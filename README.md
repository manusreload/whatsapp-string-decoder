whatsapp-string-decoder
=======================

Decode all strings in the source code of whatsapp for Java.


You can use at the command line using: java -jar wsdec.jar <source_files_apk_whatsapp> -d
